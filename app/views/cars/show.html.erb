this is where i build a new car (after i have already built my 1st one)

<%= form_for @car do |f| %>
    <%= f.text_field :make %>
    <%= f.text_field :model %>
    <%= f.text_field :year %>
    <%= f.text_field :color %>
    <%= f.submit "Create" %>
<% end %>
